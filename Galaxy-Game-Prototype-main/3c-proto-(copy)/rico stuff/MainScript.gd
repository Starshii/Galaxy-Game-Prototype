extends Node


## ////////////////////////////////////////////////////////////////////////////////////////////////
## VARIABLES
## ////////////////////////////////////////////////////////////////////////////////////////////////

# AREA INFORMATION ////////////////////////////////////////////////////////////////////////////////

@export var CurrentArea: Node3D


## ////////////////////////////////////////////////////////////////////////////////////////////////
## READY
## ////////////////////////////////////////////////////////////////////////////////////////////////

func _ready() -> void:
	Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)


## ////////////////////////////////////////////////////////////////////////////////////////////////
## PROCESS
## ////////////////////////////////////////////////////////////////////////////////////////////////

func _process(_Delta: float) -> void:
	
	# /////////////////////////////////////////////////////////////////////////////////////////////
	# PROCESS STATE
	# /////////////////////////////////////////////////////////////////////////////////////////////
	
	# RESET ///////////////////////////////////////////////////////////////////////////////////////
	
	# Resetting the tree most importantly resets the player position
	if Input.is_action_just_pressed("Reset"):
		get_tree().reload_current_scene()
	
	
	# PAUSE ///////////////////////////////////////////////////////////////////////////////////////
	
	# Check if the game is currently already paused
	if Input.is_action_just_pressed("Pause") and !get_tree().paused:
		get_tree().paused = true
		Input.set_mouse_mode(Input.MOUSE_MODE_VISIBLE)
	
	elif Input.is_action_just_pressed("Pause"):
		get_tree().paused = false
		Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)
	
	
	# FULLSCREEN //////////////////////////////////////////////////////////////////////////////////
	
	# Check if the game is already fullscreened (The enum value for fullscreened is 3)
	if Input.is_action_just_pressed("FullScreen") and DisplayServer.window_get_mode() != 3: 
		DisplayServer.window_set_mode(DisplayServer.WINDOW_MODE_FULLSCREEN)
	
	elif Input.is_action_just_pressed("FullScreen"):
		DisplayServer.window_set_mode(DisplayServer.WINDOW_MODE_WINDOWED)
